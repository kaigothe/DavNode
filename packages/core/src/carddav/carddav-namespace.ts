/**
 * The CardDAV XML namespace (RFC 6352 §5, reserved for exclusive use by
 * that specification). Every CardDAV-specific element (e.g.
 * `CARDDAV:addressbook-home-set`, `CARDDAV:addressbook`) lives in this
 * namespace, analogous to {@link DAV_NAMESPACE} for core WebDAV
 * elements.
 */
export const CARDDAV_NAMESPACE = 'urn:ietf:params:xml:ns:carddav';
