{
  "name": "@davnode/core",
  "version": "1.4.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --root ../.. --project \"$npm_package_name\"",
    "migration:generate": "npm run build && typeorm migration:generate -d dist/db/cli-data-source.js",
    "migration:run": "npm run build && typeorm migration:run -d dist/db/cli-data-source.js",
    "migration:revert": "npm run build && typeorm migration:revert -d dist/db/cli-data-source.js",
    "bootstrap": "npm run build && node dist/cli/bootstrap.js"
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "better-sqlite3": "^12.11.1",
    "mysql2": "^3.24.2",
    "pg": "^8.23.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^1.1.0",
    "xmlbuilder2": "^4.0.3"
  }
}
